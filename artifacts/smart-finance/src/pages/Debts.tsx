import { useState } from "react";
import { useListDebts, useCreateDebt, useUpdateDebt, useDeleteDebt, useGetPayoffSchedules, Debt, DebtInput } from "@workspace/api-client-react";
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { useForm } from "react-hook-form";
import { z } from "zod";
import { zodResolver } from "@hookform/resolvers/zod";
import { useQueryClient } from "@tanstack/react-query";
import { Wallet, Plus, Trash2, Pencil, Calendar, Percent, CheckCircle2, CreditCard, History, ArrowUpCircle, Zap, TrendingDown, Clock3 } from "lucide-react";
import { useToast } from "@/hooks/use-toast";

const fmt = (val: number) => new Intl.NumberFormat("ru-RU", { maximumFractionDigits: 0 }).format(Math.round(val)) + "\u00a0сом";
const debtSchema = z.object({ creditorName: z.string().min(1, "Обязательное поле"), originalAmount: z.coerce.number().min(0, "Должно быть положительным"), totalDebt: z.coerce.number().min(0, "Должно быть положительным"), termMonths: z.coerce.number().int().min(1, "Укажите срок кредита"), monthlyPayment: z.coerce.number().min(0, "Должно быть положительным"), interestRate: z.coerce.number().min(0, "Должно быть положительным"), dueDate: z.string().min(1, "Обязательное поле"), notes: z.string().optional() });
const paymentSchema = z.object({ amount: z.coerce.number().positive("Введите сумму больше нуля"), paymentType: z.enum(["monthly", "early", "full"]), paidAt: z.string().min(1, "Укажите дату"), notes: z.string().optional() });
type DebtWithLoan = Debt & { originalAmount?: number; termMonths?: number };
type DebtPayment = { id: number; amount: number; principalPaid: number; interestPaid: number; paymentType: "monthly" | "early" | "full" | string; paidAt: string; notes?: string | null };

export default function Debts() {
  const queryClient = useQueryClient();
  const { toast } = useToast();
  const { data: debts, isLoading: isLoadingDebts } = useListDebts({ query: { queryKey: ["/api/debts"] } });
  const { data: payoff, isLoading: isLoadingPayoff } = useGetPayoffSchedules({ query: { queryKey: ["/api/debts/payoff-schedules"] } });
  const createDebt = useCreateDebt(); const updateDebt = useUpdateDebt(); const deleteDebt = useDeleteDebt();
  const [isDialogOpen, setIsDialogOpen] = useState(false); const [editingDebt, setEditingDebt] = useState<DebtWithLoan | null>(null); const [paymentDebt, setPaymentDebt] = useState<DebtWithLoan | null>(null); const [payments, setPayments] = useState<DebtPayment[]>([]); const [isLoadingPayments, setIsLoadingPayments] = useState(false); const [isSavingPayment, setIsSavingPayment] = useState(false);
  const form = useForm<z.infer<typeof debtSchema>>({ resolver: zodResolver(debtSchema), defaultValues: { creditorName: "", originalAmount: 0, totalDebt: 0, termMonths: 12, monthlyPayment: 0, interestRate: 0, dueDate: "", notes: "" } });
  const paymentForm = useForm<z.infer<typeof paymentSchema>>({ resolver: zodResolver(paymentSchema), defaultValues: { amount: 0, paymentType: "monthly", paidAt: new Date().toISOString().slice(0, 10), notes: "" } });
  const refreshDebts = () => { ["/api/debts", "/api/debts/payoff-schedules", "/api/dashboard", "/api/balance", "/api/dashboard/detailed"].forEach(k => queryClient.invalidateQueries({ queryKey: [k] })); };
  const handleOpenDialog = (debt?: DebtWithLoan) => { if (debt) { setEditingDebt(debt); form.reset({ creditorName: debt.creditorName, originalAmount: debt.originalAmount || debt.totalDebt, totalDebt: debt.totalDebt, termMonths: debt.termMonths || 1, monthlyPayment: debt.monthlyPayment, interestRate: debt.interestRate, dueDate: debt.dueDate, notes: debt.notes || "" }); } else { setEditingDebt(null); form.reset({ creditorName: "", originalAmount: 0, totalDebt: 0, termMonths: 12, monthlyPayment: 0, interestRate: 0, dueDate: "", notes: "" }); } setIsDialogOpen(true); };
  const onSubmit = (data: z.infer<typeof debtSchema>) => { const payload = data as unknown as DebtInput; const done = () => { refreshDebts(); setIsDialogOpen(false); toast({ title: editingDebt ? "Долг обновлён" : "Долг добавлен" }); }; const fail = (error: unknown) => toast({ title: "Не удалось сохранить долг", description: error instanceof Error ? error.message : "Проверьте поля и попробуйте ещё раз.", variant: "destructive" }); if (editingDebt) updateDebt.mutate({ id: editingDebt.id, data: payload }, { onSuccess: done, onError: fail }); else createDebt.mutate({ data: payload }, { onSuccess: done, onError: fail }); };
  const handleDelete = (id: number) => { if (confirm("Удалить этот долг? История платежей тоже будет удалена.")) deleteDebt.mutate({ id }, { onSuccess: () => { refreshDebts(); toast({ title: "Долг удалён" }); }, onError: (error) => toast({ title: "Не удалось удалить долг", description: error instanceof Error ? error.message : "Попробуйте ещё раз.", variant: "destructive" }) }); };
  const loadPayments = async (debtId: number) => { setIsLoadingPayments(true); try { const response = await fetch(`/api/debts/${debtId}/payments`); if (!response.ok) throw new Error("Не удалось загрузить историю платежей"); setPayments(await response.json()); } catch (error) { toast({ title: error instanceof Error ? error.message : "Ошибка загрузки истории", variant: "destructive" }); } finally { setIsLoadingPayments(false); } };
  const handleOpenPayment = (debt: DebtWithLoan) => { setPaymentDebt(debt); paymentForm.reset({ amount: debt.monthlyPayment, paymentType: "monthly", paidAt: new Date().toISOString().slice(0, 10), notes: "" }); void loadPayments(debt.id); };
  const handlePaymentTypeChange = (type: "monthly" | "early" | "full") => { paymentForm.setValue("paymentType", type); if (!paymentDebt) return; if (type === "monthly") paymentForm.setValue("amount", paymentDebt.monthlyPayment); if (type === "full") paymentForm.setValue("amount", paymentDebt.totalDebt); if (type === "early") paymentForm.setValue("amount", 0); };
  const onPaymentSubmit = async (data: z.infer<typeof paymentSchema>) => { if (!paymentDebt) return; setIsSavingPayment(true); try { const response = await fetch(`/api/debts/${paymentDebt.id}/payments`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); const body = await response.json().catch(() => null); if (!response.ok) throw new Error(body?.error || "Не удалось записать платеж"); refreshDebts(); await loadPayments(paymentDebt.id); const newDebt = body?.debt; if (newDebt) setPaymentDebt(current => current ? { ...current, ...newDebt } : current); paymentForm.reset({ amount: data.paymentType === "monthly" ? paymentDebt.monthlyPayment : 0, paymentType: data.paymentType, paidAt: new Date().toISOString().slice(0, 10), notes: "" }); toast({ title: "Платёж записан", description: `С баланса списано: ${fmt(body?.payment?.amount ?? data.amount)} · Остаток долга: ${fmt(newDebt?.totalDebt ?? paymentDebt.totalDebt)}` }); } catch (error) { toast({ title: error instanceof Error ? error.message : "Ошибка платежа", variant: "destructive" }); } finally { setIsSavingPayment(false); } };
  if (isLoadingDebts || isLoadingPayoff) return <div className="space-y-6"><Skeleton className="h-12 w-64" /><Skeleton className="h-64 w-full" /></div>;
  const debtList = debts as DebtWithLoan[] | undefined;
  const activeDebts = (debtList || []).filter(d => Number(d.totalDebt) > 0.01);
  const totalDebt = activeDebts.reduce((s, d) => s + Number(d.totalDebt), 0);
  const monthlyPayments = activeDebts.reduce((s, d) => s + Number(d.monthlyPayment), 0);
  const totalOriginal = activeDebts.reduce((s, d) => s + Number(d.originalAmount ?? d.totalDebt), 0);
  const paidDown = Math.max(0, totalOriginal - totalDebt);
  const nextDebt = [...activeDebts].sort((a, b) => String(a.dueDate).localeCompare(String(b.dueDate)))[0];
  const progress = totalOriginal > 0 ? Math.min(100, Math.max(0, paidDown / totalOriginal * 100)) : 0;
  const isSnowballBetter = payoff && payoff.snowballTotalInterest <= payoff.avalancheTotalInterest;
  const daysUntil = nextDebt ? Math.ceil((new Date(`${nextDebt.dueDate}T00:00:00`).getTime() - Date.now()) / 86400000) : null;
  return <div className="space-y-8 animate-in fade-in slide-in-from-bottom-4 duration-500">
    <div className="flex flex-col md:flex-row md:items-end justify-between gap-4"><div><h1 className="text-3xl font-bold tracking-tight">Долги и кредиты</h1><p className="text-muted-foreground mt-1">Сколько должен, сколько платишь каждый месяц и какой платёж следующий.</p></div><Button size="lg" onClick={() => handleOpenDialog()} className="hover-elevate font-semibold"><Plus className="mr-2 h-4 w-4" /> Добавить кредит / долг</Button></div>
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
      <Card><CardContent className="p-5"><div className="flex items-center justify-between"><span className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Общий долг</span><TrendingDown className="h-4 w-4 text-destructive" /></div><div className="text-2xl font-bold mt-2 text-destructive">{fmt(totalDebt)}</div><p className="text-xs text-muted-foreground mt-1">Остаток по активным кредитам</p></CardContent></Card>
      <Card><CardContent className="p-5"><div className="flex items-center justify-between"><span className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Платежи / месяц</span><CreditCard className="h-4 w-4 text-primary" /></div><div className="text-2xl font-bold mt-2">{fmt(monthlyPayments)}</div><p className="text-xs text-muted-foreground mt-1">Минимальная регулярная нагрузка</p></CardContent></Card>
      <Card><CardContent className="p-5"><div className="flex items-center justify-between"><span className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Погашено</span><CheckCircle2 className="h-4 w-4 text-emerald-500" /></div><div className="text-2xl font-bold mt-2">{progress.toFixed(0)}%</div><div className="h-2 rounded-full bg-muted mt-3 overflow-hidden"><div className="h-full rounded-full bg-emerald-500" style={{ width: `${progress}%` }} /></div><p className="text-xs text-muted-foreground mt-1">{fmt(paidDown)} от первоначальной суммы</p></CardContent></Card>
      <Card className={nextDebt && daysUntil !== null && daysUntil <= 7 ? "border-amber-500/50" : ""}><CardContent className="p-5"><div className="flex items-center justify-between"><span className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Ближайший платёж</span><Clock3 className="h-4 w-4 text-amber-500" /></div>{nextDebt ? <><div className="text-base font-bold mt-2 truncate">{nextDebt.creditorName}</div><div className="text-lg font-mono font-semibold">{fmt(nextDebt.monthlyPayment)}</div><p className="text-xs text-muted-foreground mt-1">{new Date(nextDebt.dueDate).toLocaleDateString("ru-RU", { day: "numeric", month: "long" })}{daysUntil !== null ? ` · ${daysUntil < 0 ? "просрочен" : daysUntil === 0 ? "сегодня" : `через ${daysUntil} дн.`}` : ""}</p></> : <p className="text-sm text-muted-foreground mt-3">Активных долгов нет</p>}</CardContent></Card>
    </div>
    <Card><CardHeader><CardTitle>Кредиты — простая картина</CardTitle><CardDescription>Здесь главное — остаток долга, обязательный платёж и ближайшая дата. Нажми «Погашение», чтобы реально списать деньги с текущего баланса.</CardDescription></CardHeader><CardContent>{activeDebts.length ? <div className="grid grid-cols-1 md:grid-cols-2 gap-4">{activeDebts.map(debt => { const original = Number(debt.originalAmount ?? debt.totalDebt); const pct = original > 0 ? Math.min(100, Math.max(0, ((original - Number(debt.totalDebt)) / original) * 100)) : 0; const days = Math.ceil((new Date(`${debt.dueDate}T00:00:00`).getTime() - Date.now()) / 86400000); return <div key={debt.id} className="rounded-xl border p-5 space-y-4"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><div className="text-lg font-bold truncate">{debt.creditorName}</div><div className="text-xs text-muted-foreground mt-1">{debt.notes || "Кредит / долг"}</div></div><Button size="sm" onClick={() => handleOpenPayment(debt)}><CreditCard className="mr-2 h-4 w-4" />Погасить</Button></div><div className="grid grid-cols-3 gap-3"><div><div className="text-xs text-muted-foreground">Остаток</div><div className="font-mono font-bold text-destructive">{fmt(debt.totalDebt)}</div></div><div><div className="text-xs text-muted-foreground">В месяц</div><div className="font-mono font-bold">{fmt(debt.monthlyPayment)}</div></div><div><div className="text-xs text-muted-foreground">Ставка</div><div className="font-mono font-bold">{debt.interestRate}%</div></div></div><div><div className="flex justify-between text-xs text-muted-foreground mb-1"><span>Погашено</span><span>{pct.toFixed(0)}%</span></div><div className="h-2 rounded-full bg-muted overflow-hidden"><div className="h-full rounded-full bg-primary" style={{ width: `${pct}%` }} /></div></div><div className="flex items-center justify-between text-xs text-muted-foreground"><span className="flex items-center gap-1"><Calendar className="h-3 w-3" />Следующий платёж: {new Date(debt.dueDate).toLocaleDateString("ru-RU", { day: "numeric", month: "short" })}</span><span className={days < 0 ? "text-destructive font-semibold" : days <= 7 ? "text-amber-500 font-semibold" : ""}>{days < 0 ? "просрочен" : days === 0 ? "сегодня" : `через ${days} дн.`}</span></div><div className="flex justify-end gap-1"><Button variant="ghost" size="icon" onClick={() => handleOpenDialog(debt)} title="Редактировать"><Pencil className="h-4 w-4" /></Button><Button variant="ghost" size="icon" onClick={() => handleDelete(debt.id)} title="Удалить"><Trash2 className="h-4 w-4" /></Button></div></div> })}</div> : <div className="text-center py-10 text-muted-foreground"><Wallet className="h-12 w-12 mx-auto mb-3 opacity-20" /><p className="text-base">Долгов не записано. Добавьте первый!</p></div>}</CardContent></Card>
    <Dialog open={isDialogOpen} onOpenChange={setIsDialogOpen}><DialogContent className="sm:max-w-[520px]"><DialogHeader><DialogTitle>{editingDebt ? "Редактировать кредит / долг" : "Новый кредит / долг"}</DialogTitle></DialogHeader><Form {...form}><form onSubmit={form.handleSubmit(onSubmit, () => toast({ title: "Проверьте поля", description: "Заполните банк, остаток, платёж и дату.", variant: "destructive" }))} className="space-y-4 mt-2"><FormField control={form.control} name="creditorName" render={({ field }) => <FormItem><FormLabel>Банк / кредитор</FormLabel><FormControl><Input {...field} placeholder="МБанк, Оптима, МФО, рассрочка…" /></FormControl><FormMessage /></FormItem>} /><div className="grid grid-cols-2 gap-4"><FormField control={form.control} name="originalAmount" render={({ field }) => <FormItem><FormLabel>Первоначальная сумма</FormLabel><FormControl><Input type="number" min="0" step="1" {...field} /></FormControl><FormMessage /></FormItem>} /><FormField control={form.control} name="totalDebt" render={({ field }) => <FormItem><FormLabel>Остаток сейчас</FormLabel><FormControl><Input type="number" min="0" step="1" {...field} /></FormControl><FormMessage /></FormItem>} /></div><div className="grid grid-cols-2 gap-4"><FormField control={form.control} name="monthlyPayment" render={({ field }) => <FormItem><FormLabel>Обязательный платёж / мес.</FormLabel><FormControl><Input type="number" min="0" step="1" {...field} /></FormControl><FormMessage /></FormItem>} /><FormField control={form.control} name="termMonths" render={({ field }) => <FormItem><FormLabel>Срок, месяцев</FormLabel><FormControl><Input type="number" min="1" step="1" {...field} /></FormControl><FormMessage /></FormItem>} /></div><div className="grid grid-cols-2 gap-4"><FormField control={form.control} name="interestRate" render={({ field }) => <FormItem><FormLabel>Ставка, % годовых</FormLabel><FormControl><Input type="number" min="0" step="0.1" {...field} /></FormControl><FormMessage /></FormItem>} /><FormField control={form.control} name="dueDate" render={({ field }) => <FormItem><FormLabel>Следующая дата платежа</FormLabel><FormControl><Input type="date" {...field} /></FormControl><FormMessage /></FormItem>} /></div><FormField control={form.control} name="notes" render={({ field }) => <FormItem><FormLabel>Комментарий</FormLabel><FormControl><Input {...field} placeholder="Ипотека, рассрочка, кредитная карта…" /></FormControl><FormMessage /></FormItem>} /><div className="flex justify-end"><Button type="submit" size="lg" disabled={createDebt.isPending || updateDebt.isPending}>{createDebt.isPending || updateDebt.isPending ? "Сохраняем…" : editingDebt ? "Сохранить изменения" : "Добавить кредит"}</Button></div></form></Form></DialogContent></Dialog>
    <Dialog open={Boolean(paymentDebt)} onOpenChange={open => !open && setPaymentDebt(null)}><DialogContent className="sm:max-w-[680px]"><DialogHeader><DialogTitle className="flex items-center gap-2"><CreditCard className="h-5 w-5" /> Погашение: {paymentDebt?.creditorName}</DialogTitle></DialogHeader>{paymentDebt && <div className="space-y-5"><div className="grid grid-cols-3 gap-3"><div className="rounded-md border bg-muted/30 p-3"><div className="text-xs text-muted-foreground">Остаток долга</div><div className="text-xl font-bold text-destructive">{fmt(paymentDebt.totalDebt)}</div></div><div className="rounded-md border bg-muted/30 p-3"><div className="text-xs text-muted-foreground">Платёж / мес.</div><div className="text-xl font-bold">{fmt(paymentDebt.monthlyPayment)}</div></div><div className="rounded-md border bg-muted/30 p-3"><div className="text-xs text-muted-foreground">Ставка</div><div className="text-xl font-bold">{paymentDebt.interestRate}%</div></div></div><div className="rounded-lg border bg-muted/20 p-3 text-sm text-muted-foreground">Платёж ниже одновременно уменьшит долг, спишет деньги с текущего баланса и попадёт в историю операций.</div><Form {...paymentForm}><form onSubmit={paymentForm.handleSubmit(onPaymentSubmit, () => toast({ title: "Проверьте сумму платежа", variant: "destructive" }))} className="space-y-4"><FormField control={paymentForm.control} name="paymentType" render={({ field }) => <FormItem><FormLabel>Что делаем?</FormLabel><div className="grid grid-cols-3 gap-2"><Button type="button" variant={field.value === "monthly" ? "default" : "outline"} onClick={() => handlePaymentTypeChange("monthly")}><Calendar className="mr-2 h-4 w-4" />Ежемесячный</Button><Button type="button" variant={field.value === "early" ? "default" : "outline"} onClick={() => handlePaymentTypeChange("early")}><Zap className="mr-2 h-4 w-4" />Досрочный</Button><Button type="button" variant={field.value === "full" ? "default" : "outline"} onClick={() => handlePaymentTypeChange("full")}><CheckCircle2 className="mr-2 h-4 w-4" />Закрыть</Button></div><FormMessage /></FormItem>} /><div className="grid grid-cols-2 gap-4"><FormField control={paymentForm.control} name="amount" render={({ field }) => <FormItem><FormLabel>Фактическая сумма списания</FormLabel><FormControl><Input type="number" min="1" step="1" {...field} /></FormControl><FormMessage /></FormItem>} /><FormField control={paymentForm.control} name="paidAt" render={({ field }) => <FormItem><FormLabel>Дата платежа</FormLabel><FormControl><Input type="date" {...field} /></FormControl><FormMessage /></FormItem>} /></div><FormField control={paymentForm.control} name="notes" render={({ field }) => <FormItem><FormLabel>Комментарий</FormLabel><FormControl><Input {...field} placeholder="Например: платёж за сентябрь" /></FormControl><FormMessage /></FormItem>} /><div className="flex justify-end"><Button type="submit" disabled={isSavingPayment}>{isSavingPayment ? "Записываем…" : <><ArrowUpCircle className="mr-2 h-4 w-4" />Записать платёж</>}</Button></div></form></Form><div><div className="flex items-center gap-2 mb-3"><History className="h-4 w-4" /><h3 className="font-semibold">История платежей</h3></div>{isLoadingPayments ? <Skeleton className="h-24 w-full" /> : payments.length === 0 ? <div className="rounded-md border border-dashed p-5 text-center text-sm text-muted-foreground">Платежей пока нет.</div> : <div className="rounded-md border divide-y max-h-56 overflow-y-auto">{payments.map(payment => <div key={payment.id} className="p-3 flex items-center justify-between gap-4 text-sm"><div><div className="font-medium">{payment.paymentType === "monthly" ? "Ежемесячный платёж" : payment.paymentType === "early" ? "Досрочное погашение" : "Полное погашение"}</div><div className="text-xs text-muted-foreground">{new Date(payment.paidAt).toLocaleDateString("ru-RU")} {payment.notes ? `· ${payment.notes}` : ""}</div></div><div className="text-right"><div className="font-mono font-semibold">{fmt(payment.amount)}</div><div className="text-xs text-muted-foreground">тело {fmt(payment.principalPaid)} · проценты {fmt(payment.interestPaid)}</div></div></div>)}</div>}</div></div>}</DialogContent></Dialog>
    {payoff && activeDebts.length > 1 && <div className="grid grid-cols-1 lg:grid-cols-2 gap-6"><Card className={`border-2 ${isSnowballBetter ? "border-emerald-500 bg-emerald-500/5" : "border-border"}`}><CardHeader className="pb-2"><div className="flex justify-between items-start"><div><CardTitle className="flex items-center gap-2">Снежный ком {isSnowballBetter && <CheckCircle2 className="h-5 w-5 text-emerald-500" />}</CardTitle><CardDescription>Сначала меньший долг — психологически быстрее освобождаешься от кредитов.</CardDescription></div>{isSnowballBetter && <span className="bg-emerald-500 text-white text-[10px] uppercase font-bold px-2 py-1 rounded-sm">Рекомендуется</span>}</div></CardHeader><CardContent><div className="grid grid-cols-2 gap-4 mb-4"><div className="bg-background p-3 rounded-md border"><div className="text-xs text-muted-foreground">Срок</div><div className="text-2xl font-bold">{payoff.snowballTotalMonths} <span className="text-sm font-normal text-muted-foreground">мес</span></div></div><div className="bg-background p-3 rounded-md border"><div className="text-xs text-muted-foreground">Переплата</div><div className="text-2xl font-bold text-destructive">{fmt(payoff.snowballTotalInterest)}</div></div></div><div className="text-xs font-bold text-muted-foreground uppercase tracking-wider mb-2">Порядок</div>{payoff.snowball.map((p, i) => <div key={p.debtId} className="flex items-center text-sm border-b last:border-0 pb-2 last:pb-0"><span className="w-5 h-5 rounded-full bg-primary/20 text-primary flex items-center justify-center text-[10px] font-bold mr-3">{i + 1}</span><span className="font-medium flex-1">{p.creditorName}</span><span className="text-muted-foreground">{p.monthsToPayoff} мес</span></div>)}</CardContent></Card><Card className={`border-2 ${!isSnowballBetter ? "border-emerald-500 bg-emerald-500/5" : "border-border"}`}><CardHeader className="pb-2"><div className="flex justify-between items-start"><div><CardTitle className="flex items-center gap-2">Лавина {!isSnowballBetter && <CheckCircle2 className="h-5 w-5 text-emerald-500" />}</CardTitle><CardDescription>Сначала самая высокая ставка — обычно меньше переплата по процентам.</CardDescription></div>{!isSnowballBetter && <span className="bg-emerald-500 text-white text-[10px] uppercase font-bold px-2 py-1 rounded-sm">Рекомендуется</span>}</div></CardHeader><CardContent><div className="grid grid-cols-2 gap-4 mb-4"><div className="bg-background p-3 rounded-md border"><div className="text-xs text-muted-foreground">Срок</div><div className="text-2xl font-bold">{payoff.avalancheTotalMonths} <span className="text-sm font-normal text-muted-foreground">мес</span></div></div><div className="bg-background p-3 rounded-md border"><div className="text-xs text-muted-foreground">Переплата</div><div className="text-2xl font-bold text-destructive">{fmt(payoff.avalancheTotalInterest)}</div></div></div><div className="text-xs font-bold text-muted-foreground uppercase tracking-wider mb-2">Порядок</div>{payoff.avalanche.map((p, i) => <div key={p.debtId} className="flex items-center text-sm border-b last:border-0 pb-2 last:pb-0"><span className="w-5 h-5 rounded-full bg-primary/20 text-primary flex items-center justify-center text-[10px] font-bold mr-3">{i + 1}</span><span className="font-medium flex-1">{p.creditorName}</span><span className="text-muted-foreground">{p.monthsToPayoff} мес</span></div>)}</CardContent></Card></div>}
  </div>;
}
